[project]
name = "prj649-ml"
version = "0.1.0"
description = "ML training and evaluation for ScholarPulse"
requires-python = ">=3.13"
dependencies = [
    "joblib>=1.5.3",
    "matplotlib>=3.10.0",
    "numpy>=2.5.2",
    "pandas>=3.0.5",
    "scikit-learn==1.6.1",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]

[tool.uv]
package = false
