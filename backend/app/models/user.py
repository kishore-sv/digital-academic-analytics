"""User model (stub).

All tenant-owned records include institution_id for multi-tenant isolation.
"""

# TODO: SQLAlchemy model
# Fields: id, institution_id, email, password_hash, role, is_login_enabled, created_at
