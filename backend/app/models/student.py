"""Student model (stub).

Tenant-owned via institution_id.
Login: roll_number + password.
"""

# TODO: SQLAlchemy model
# Fields: id, institution_id, user_id, roll_number, name, department_id,
#         program, semester, is_login_enabled
